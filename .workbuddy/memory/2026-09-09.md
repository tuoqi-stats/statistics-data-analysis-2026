# 2026-09-09

## 新建项目级 skill：concept-material-generator
- 路径：`.workbuddy/skills/concept-material-generator/SKILL.md`
- 功能：输入任意 AI 概念名词 → 生成五段式学习文档（概念个人解释 / 核心机制或组成 / 具体应用场景 / 易混淆问题或使用边界 / 可核查资料来源链接）→ 保存到 `learning-materials/`
- YAML frontmatter：name、description、allowed-tools: Read, Write、agent_created: true
- 细节：用户消息中概念名里的连字符是 U+2011（不间断连字符），实际文件路径统一改用 ASCII 连字符（-）

## 生成概念关系文档
- 新增 `learning-materials/concept-relationship.md`：串联 Agent、大模型上下文、Skill 三个已有概念，阐述三者三角协作关系
- 核心论点：能力 = 基座模型 × 上下文管理 × 技能注入（乘法关系，缺一不可）
- 四条咬合机制：加载链 Skill→Context→Agent / 上下文作唯一交汇面 / 渐进式披露作节流阀 / 两类知识一个窗口的张力
- README.md 索引已同步更新

## 生成项目 README.md
- 覆写仓库根目录 README.md，从 2 行扩展为完整项目文档
- 含五部分：项目简介、目录结构、Skill 调用方式、已完成文档清单（6 份）、作业完成说明
- 标注项目本身就是 Agent × Context × Skill 三角关系的实践样本

## 推送到远程仓库
- git push 502 失败（沙箱代理），改用 Git Data API 绕过法推送成功
- 远程 commit SHA: a311311a（API 创建），本地 commit SHA: fdc47de（git 创建），内容一致
- 12 个文件全部上传：.gitignore / README.md / .workbuddy/* / learning-materials/*
- 本地引用同步：手动写 loose ref (.git/refs/remotes/origin/main → fdc47de)，git status 干净
- 注意：本地与远程 commit SHA 不同，网络恢复后 git fetch 会发现分叉，内容等价无冲突

## 目录与配置
- 新建 `learning-materials/`（含 README.md，说明命名规范与五部分结构约定）
- 仓库已有 Python 模板 .gitignore，末尾追加了密钥 / API key 屏蔽区块（*.key、*.pem、*apikey*、credentials.json、.env.* 等），未覆盖原有规则

## 二次推送：统一 commit 历史
- 网络恢复，git push 可连通（不再 502），但因本地 fdc47de 与远程 a311311a SHA 不同被拒（non-fast-forward）
- 逐文件对比 blob SHA：12 个文件中 10 个完全一致，.gitignore 与 README.md 因行尾符不同（远程 CRLF / 本地 LF，core.autocrlf=true）SHA 不同
- 执行 `git push --force origin main`，用本地规范版本（LF）覆盖远程，统一历史为 0bf3d7c → fdc47de → 95ebc83
- 再次修复 origin/main 跟踪引用卡住问题（.git/refs/remotes/origin/ 目录缺失 → mkdir + 写 loose ref），git status 干净
- 教训：API 推送时直接用工作区文件内容（CRLF），与 git 正常提交（LF 存储）会产生行尾符分叉；网络恢复后优先用 git push --force 统一
